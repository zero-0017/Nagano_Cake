<div class="container">
  <div class="row">
    <div class="col-5">
      <h4 class="m-3">ジャンル編集</h4>
      <%= form_with model:@genre, url: admin_genre_path, local:true do |f| %>
        <p class="my-5 mx-2">ジャンル名
          <%= f.text_field :name, class: "mx-2" %>
          <%= f.submit '変更を保存', class: "btn btn-success mx-2" %>
        </p>
      <% end %>
    </div>
  </div>
</div>