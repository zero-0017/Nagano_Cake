<div class="container">
  <h4 class="ml-5 mb-4"><strong>商品詳細</strong></h4>
  <div class="row shadow-lg">

    <div class="col-md-4 offset-md-1 mt-5 mx-auto">
      <%= image_tag @item.image, :size => '250' %>
    </div>
    <div class="col-6 px-0 mt-5 mx-auto">
      <table class="table table-borderless">
        <tr>
          <th>商品名</th><td><%= @item.name %></td>
        </tr>
        <tr>
          <th>商品説明</th><td><%= safe_join(@item.introduction.split("\n"),tag(:br)) %></td>
        </tr>
        <tr>
          <th>ジャンル</th><td><%= @item.genre.name %></td>
        </tr>
        <tr valign="middle">
          <th>税込価格</br>(税抜価格)</th>
          <td>¥<%= @item.add_tax_price.to_s(:dalimited) %><br>(¥<%= @item.price.to_s(:dalimited) %>)</td>
        </tr>
        <tr>
          <th>販売ステータス</th>
          <td>
            <% if @item.is_active %>
              <p class="text-success font-weight-bold">販売中</p>
            <% else %>
              <p class="text-danger font-weight-bold">販売停止中</p>
            <% end %>
          <td>
        </tr>
      </table>
    </div>
  </div>
  <div class="text-right mr-5 mt-3">
        <%= link_to '編集する', edit_admin_item_path(@item.id), class: "btn btn-success mr-5" %>
      </div>
</div>