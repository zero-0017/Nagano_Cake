<h3>注文情報入力</h3>

<%= form_with model: @order, url: confirm_orders_path, method: :post do |f| %>

<h4>支払い方法</h4>
  <%= f.radio_button :payment_option, Order.payment_methods.key(0) %>
  <%= f.label :payment_option, Order.payment_methods[:credit_card] %>

  <%= f.radio_button :payment_method, Order.payment_methods.key(1) %>
  <%= f.label :payment_method, Order.payment_methods[:transfer]%>

<h4>お届け先</h4>
  <%= f.radio_button :address_number, 0, checked: true %>
  <%= f.label "ご自身の住所" %>
  <%= "#{current_customer.postal_code} #{current_customer.address}" %><br>
  <%= "#{current_customer.family_name} #{current_customer.first_name}" %>


  <%= f.radio_button :address_number, 1 %>
  <%= f.label "登録先済みの住所から選択" %>

  <%= f.collection_select :customer_id, @addresses, :id, :full_adresses %>

  <%= f.radio_button :address_number, 2 %>
  <%= f.label "新しいお届け先" %>
    <table>
      <tr>
        <td><%= f.label :shipping_post_code, "郵便番号(ハイフンなし)" %></td>
        <td><%= f.text_field :shipping_post_code %></td>
      </tr>

      <tr>
        <td><%= f.label :shipping_address, "住所" %></td>
        <td><%= f.text_field :shipping_address %></td>
      </tr>

      <tr>
        <td><%= f.label :shipping_name, "宛名" %></td>
        <td><%= f.text_field :shipping_name %></td>
      </tr>
    </table>

  <%= f.submit "確認画面へ進む", class: "btn btn-primary" %>
<% end %>